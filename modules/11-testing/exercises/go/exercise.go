// Package exercises: Module 11 reader task. RED until you implement Dedup.
package exercises

// TODO(reader): return xs with CONSECUTIVE duplicates removed, preserving order.
// Dedup([1,1,2,2,2,3,1]) => [1,2,3,1]. Do not mutate xs. An empty or one-element
// slice is returned as-is (a fresh copy is fine). The property test asserts
// Dedup is idempotent: Dedup(Dedup(xs)) has the same elements as Dedup(xs).
func Dedup(xs []int) []int {
	return nil // replace me
}
